# Migración a monorepo (en curso)

La web de este repo se ha fusionado en el repo privado del proyecto
(rama `claude/monorepo`), junto con el pipeline de datos. Este repo público
sigue sirviendo producción en Vercel hasta completar el cutover (checklist
en el MIGRATION.md del monorepo).

**No añadir features nuevas aquí** — solo hotfixes de producción hasta el cutover.
